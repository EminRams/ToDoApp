import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  roundedButton: {
    backgroundColor: 'gray',
    opacity: 0.8,
    padding: 10,
  },
  buttonText: {
    color: 'white',
    textAlign: 'center',
  },
  // Agrega más estilos globales aquí si es necesario
});
